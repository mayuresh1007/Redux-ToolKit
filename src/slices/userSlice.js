import { createSlice } from "@reduxjs/toolkit";

const initialState = {
  user: [],
};

export const userSlice = createSlice({
    name:'user', // this will represent in redux dev tool
    initialState,
    reducers:{
        
    }
})